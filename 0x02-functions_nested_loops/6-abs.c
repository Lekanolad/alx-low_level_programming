#include "main.h"
/**
 * _abs - main function to compute the absolute value of given integer
 *
 * @i: integer to make absolute
 * Return: int or negative times integer
 *
 */

int _abs(int i)
{
	if (i > 0)
	{
		return (n);
	}
	else
	{
		return (-i);
	}
}
